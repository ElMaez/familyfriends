import Button from "./Button";

const Header = () => {
  return (
    <header>
      <h2>FamilyFriends</h2>
      <Button
        varient="one"
        size="one"
        type="icon"
        text="this is first button"
      ></Button>
    </header>
  );
};

export default Header;
